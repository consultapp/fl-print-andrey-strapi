'use strict';

/**
 * czena-metra-pogonnogo service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::czena-metra-pogonnogo.czena-metra-pogonnogo');
