'use strict';

/**
 * czena-metra-pogonnogo router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::czena-metra-pogonnogo.czena-metra-pogonnogo');
