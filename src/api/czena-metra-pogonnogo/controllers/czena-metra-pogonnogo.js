'use strict';

/**
 * czena-metra-pogonnogo controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::czena-metra-pogonnogo.czena-metra-pogonnogo');
